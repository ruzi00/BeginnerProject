<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.beginnerproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/money_lost"
        android:label="@string/app_name"
        android:logo="@mipmap/money_lost"
        android:roundIcon="@mipmap/money_lost"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".rokokActivity" />
        <activity android:name=".lampuActivity" />
        <activity android:name=".pcActivity" />
        <activity android:name=".NumbersActivity" />
        <activity android:name=".AgeActivity" />
    </application>

</manifest>